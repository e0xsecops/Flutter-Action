/// The synthetic corpus now lives in `lib/features/diagnostics/` so the debug
/// diagnostics screen can seed a device with it. Tests keep importing it from
/// here.
library;

export 'package:action_app/features/diagnostics/scale_fixtures.dart';
